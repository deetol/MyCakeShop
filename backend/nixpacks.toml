providers = ["php"]

[phases.setup]
nixPkgs = ["...", "nodejs_22"]

[phases.build]
cmds = ["npm install --ignore-scripts", "npm run build"]

[start]
cmd = "php artisan config:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
